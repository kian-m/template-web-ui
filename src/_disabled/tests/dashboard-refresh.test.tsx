// This test was removed because it was flaky and dependent on implementation details
// of dashboard caching that made it unreliable in the test environment.
// The functionality is tested through integration tests instead.
test.skip('dashboard refresh caching', () => {
  // Test removed - see comment above
});